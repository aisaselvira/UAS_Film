<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UAS_Film" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.UAS_Film.WithActionBar"
        parent="Theme.Material3.DayNight">
        <item name="android:fontFamily">@font/poppinssemibold</item>
        <item name="android:color">@color/white</item>
        <item name="android:popupMenuStyle">@style/MyPopupMenuStyle</item>
    </style>

    <!-- Define a custom style for the popup menu -->
    <style name="MyPopupMenuStyle" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@color/youngpurple2</item>
    </style>
</resources>
